#include "binary_trees.h"

/**
 * sorted_array_to_avl - takes a sorted array and its
 *		size as input and returns an AVL tree.
 * @array: A pointer to the first element of the
 *		sorted array.
 * @size: The size parameter represents the number
 *		of elements in the array.
 *
 * Return: pointer to (avl_t *)
 */
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	avl_t *sorted = NULL;

	(void)array, (void)size;

	return (sorted);
}
