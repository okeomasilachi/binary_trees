#include "binary_trees.h"

/**
 * binary_tree_rotate_right - rotates a binary tree to
 *		the right.
 * @tree: A pointer to the root node of the binary
 *		tree that needs to be rotated right.
 *
 * Return: a pointer to a binary_tree_t structure.
 */
binary_tree_t *binary_tree_rotate_right(binary_tree_t *tree)
{
	binary_tree_t *tmp = NULL;

	return (tmp);
}
