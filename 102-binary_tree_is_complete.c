#include "binary_trees.h"

/**
 * binary_tree_is_complete - checks if a binary tree
 *		is complete.
 * @tree: a pointer to a binary tree.
 *
 * Return: int
 */
int binary_tree_is_complete(const binary_tree_t *tree)
{
	(void)tree;

	return (0);
}
