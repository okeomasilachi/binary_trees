#include "binary_trees.h"

/**
 * heap_insert - inserts a new node with a given
 *		value into an HEAP tree.
 * @tree: A double pointer to the root of the
 *		HEAP tree.
 * @value: integer value that we want to insert
 *		into the HEAP tree.
 *
 * Return: a pointer to an heap_t structure.
 */
heap_t *heap_insert(heap_t **tree, int value)
{
	heap_t *heap = NULL;

	(void)tree, (void)value;

	return (heap);
}
