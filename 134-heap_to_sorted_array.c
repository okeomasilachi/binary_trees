#include "binary_trees.h"

/**
 * heap_to_sorted_array - takes a heap and returns a
 *		sorted array.
 * @heap: A pointer to a heap data structure.
 * @size: A pointer to a variable that will store
 *		the size of the sorted array.
 *
 * Return: a pointer to an integer array.
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{
	int *arr = NULL;

	(void)heap, (void)size;

	return (arr);
}
