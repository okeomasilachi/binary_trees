#include "binary_trees.h"

/**
 * avl_insert - inserts a new node with a given
 *		value into an AVL tree.
 * @tree: A double pointer to the root of the
 *		AVL tree.
 * @value: integer value that we want to insert
 *		into the AVL tree.
 *
 * Return: a pointer to an avl_t structure.
 */
avl_t *avl_insert(avl_t **tree, int value)
{
	avl_t *avl = NULL;

	return (avl);
}
