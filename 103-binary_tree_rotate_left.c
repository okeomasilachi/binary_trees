#include "binary_trees.h"

/**
 * binary_tree_rotate_left - rotates a binary tree to
 *		the left.
 * @tree: A pointer to the root node of the binary
 *		tree that needs to be rotated left.
 *
 * Return: a pointer to a binary_tree_t structure.
 */
binary_tree_t *binary_tree_rotate_left(binary_tree_t *tree)
{
	binary_tree_t *tmp = NULL;

	(void)tree;

	return (tmp);
}
