#include "binary_trees.h"

/**
 * bst_search - searches for a specific value in a
 *		binary search tree.
 * @tree: A pointer to the root of the binary
 *		search tree.
 * @value: The value to search for in the binary
 *		search tree.
 *
 * Return: pointer to a binary search tree (bst_t).
 */
bst_t *bst_search(const bst_t *tree, int value)
{
	bst_t *bst = NULL;

	return (bst);
}
