#include "binary_trees.h"

/**
 * heap_extract - extracts the root node of a Max
 *		Binary Heap
 * @root: double pointer to a heap_t structure.
 *
 * Return: int
 */
int heap_extract(heap_t **root)
{
	return (0);
}
