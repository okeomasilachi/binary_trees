#include "binary_trees.h"

/**
 * avl_remove - is used to remove a node with a
 *		specific value from a binary search tree.
 * @root: The root node of the binary search tree
 *		from which the value needs to be removed.
 * @value: value of the node that needs to be
 *		removed from the binary search tree.
 *
 * Return: pointer to a binary search tree (avl_t).
 */
avl_t *avl_remove(avl_t *root, int value)
{
	avl_t *avl = NULL;

	return (avl);
}
